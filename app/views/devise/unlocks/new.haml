= simple_form_for(resource, :as => resource_name, :url => unlock_path(resource_name), :html => { :method => :post, :class => 'form-signin' }) do |f|
  %h2.form-signin-heading Resend unlock instructions
  = f.error_notification
  = f.full_error :unlock_token
  = f.input :email, :required => true, :autofocus => true, :input_html => { :class => 'input-block-level' }
  = f.button :submit, "Resend unlock instructions", :input_html => { :class => 'btn btn-large btn-primary'}
  = render "devise/shared/links"