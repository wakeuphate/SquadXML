= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { :class => 'form-signin' }) do |f|
  %h2.form-signin-heading Sign in
  = f.input :email, :required => false, :autofocus => true, :input_html => { :class => 'input-block-level' }
  = f.input :password, :required => false, :input_html => { :class => 'input-block-level' }
  = f.input :remember_me, :as => :boolean, :label => false, :inline_label => "Remember me" if devise_mapping.rememberable?
  = f.button :submit, "Sign in", :input_html => { :class => 'btn btn-large btn-primary'}
  = render "devise/shared/links"